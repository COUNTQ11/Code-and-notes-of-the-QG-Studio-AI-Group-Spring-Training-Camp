# 一.线性代数核心笔记

## 1. 矩阵运算

矩阵是二维数组，常用大写字母表示（如 \(A\)）。矩阵运算包括加法、数乘、乘法、转置等。

### 1.1 加法与数乘
- **加法**：同型矩阵对应元素相加。  
  \[$(A + B)_{ij} = A_{ij} + B_{ij}$\]
- **数乘**：标量 \(c\) 乘以矩阵每个元素。  
  \[$(cA)_{ij} = c \cdot A_{ij}$\]

### 1.2 矩阵乘法
- 设 \($A \in \mathbb{R}^{m \times n}$\)，\($B \in \mathbb{R}^{n \times p}$\)，乘积 \($C = AB \in \mathbb{R}^{m \times p}$\) 满足：
  \[
  $C_{ij} = \sum_{k=1}^{n} A_{ik} B_{kj}$
  \]
- **性质**：结合律、分配律，但一般不满足交换律。

### 1.3 转置
- 转置 \($A^{\mathsf{T}}) 将行列互换：((A^{\mathsf{T}})_{ij} = A_{ji}$\)。  
- 性质：\($(AB)^{\mathsf{T}} = B^{\mathsf{T}} A^{\mathsf{T}}$\)。

---

## 2. 矩阵与向量的关系

向量可视为 \($n \times 1$\) 列矩阵或 \($1 \times n$\) 行矩阵。

- **矩阵与向量的乘积**：\($A\mathbf{x}$\) 表示矩阵 \(A\) 对向量 \($\mathbf{x}$\) 的线性变换，结果是一个新向量。
- 
- **视角**：矩阵的行向量与向量的点乘得到输出的各个分量。

---

## 3. 线性变换

线性变换 \($T: \mathbb{R}^n \to \mathbb{R}^m$\) 满足：
- \($T(\mathbf{u} + \mathbf{v}) = T(\mathbf{u}) + T(\mathbf{v})$\)
- \(T($c\mathbf{u}) = c T(\mathbf{u}$))

任何线性变换都可以用一个矩阵 \(A \in \mathbb{R}^{m \times n}\) 表示：
\[
$T(\mathbf{x}) = A\mathbf{x}$
\]

常见的线性变换：
- 旋转、缩放、剪切、投影。
- 矩阵的列向量是基向量的像。

---

## 4. 点乘（内积）

点乘是两个向量对应分量乘积之和。

- **定义**：\($\mathbf{a} \cdot \mathbf{b} = \sum_{i=1}^{n} a_i b_i = \mathbf{a}^{\mathsf{T}} \mathbf{b}$\)。
- **几何意义**：\($\mathbf{a} \cdot \mathbf{b} = \|\mathbf{a}\| \|\mathbf{b}\| \cos\theta$\)，其中 \($\theta$\) 是两向量夹角。
- **性质**：对称性、双线性、正定性。

---

## 5. 叉乘（向量积）

叉乘仅定义在三维空间中，结果是一个向量。

- **定义**：\($\mathbf{a} \times \mathbf{b} = \begin{pmatrix} a_2 b_3 - a_3 b_2 \\ a_3 b_1 - a_1 b_3 \\ a_1 b_2 - a_2 b_1 \end{pmatrix}$\)。
- **几何意义**：\($\|\mathbf{a} \times \mathbf{b}\| = \|\mathbf{a}\| \|\mathbf{b}\| \sin\theta$\)，方向垂直于 \($\mathbf{a}, \mathbf{b}$\) 所在平面，满足右手定则。
- **性质**：反交换性 \($\mathbf{a} \times \mathbf{b} = -(\mathbf{b} \times \mathbf{a})$\)；分配律。

---

## 6. 矩阵求导

矩阵求导是多元微积分的重要工具，常用于优化和机器学习。

### 6.1 标量对向量的导数
- 设 \($f(\mathbf{x}) : \mathbb{R}^n \to \mathbb{R}$\)，则梯度为列向量：
  \[
  $\nabla_{\mathbf{x}} f = \frac{\partial f}{\partial \mathbf{x}} = \begin{pmatrix} \frac{\partial f}{\partial x_1} \\ \vdots \\ \frac{\partial f}{\partial x_n} \end{pmatrix}$
  \]

### 6.2 向量对向量的导数
- 设 $(\mathbf{y} = \mathbf{f}(\mathbf{x}) : \mathbb{R}^n \to \mathbb{R}^m)$，其中  
  \($\mathbf{x} = [x_1, x_2, \dots, x_n]^{\mathsf{T}}$\)，  
  \($\mathbf{y} = [y_1, y_2, \dots, y_m]^{\mathsf{T}}$\)。

- **Jacobian 矩阵**（分子布局）定义为：
  \[
  $\frac{\partial \mathbf{y}}{\partial \mathbf{x}} =$

  $\frac{\partial y_1}{\partial x_1} & \frac{\partial y_1}{\partial x_2} & \cdots & \frac{\partial y_1}{\partial x_n} \\[4pt]$
  $\frac{\partial y_2}{\partial x_1} & \frac{\partial y_2}{\partial x_2} & \cdots & \frac{\partial y_2}{\partial x_n} \\[4pt]$
  $\vdots & \vdots & \ddots & \vdots \\[4pt]$

  \]
  即第 \(i\) 行第 \(j\) 列元素为 \($\frac{\partial y_i}{\partial x_j}$\)。

### 6.3 常用公式
- 线性形式：\($\frac{\partial (\mathbf{a}^{\mathsf{T}} \mathbf{x})}{\partial \mathbf{x}} = \mathbf{a}$\)
- 二次型：\($\frac{\partial (\mathbf{x}^{\mathsf{T}} A \mathbf{x})}{\partial \mathbf{x}} = (A + A^{\mathsf{T}}) \mathbf{x}$\)；若 \(A\) 对称，则 \($= 2A\mathbf{x}$\)

### 6.4 矩阵对标量的导数
- 逐元素求导，结果仍为矩阵。

---

## 总结
- 矩阵是线性代数的核心，用于表示线性变换和数据。
- 点乘和叉乘分别对应内积和外积，有明确的几何意义。
- 矩阵求导是优化算法（如梯度下降）的基础。

# 二.思考题

# 多智能体均值一致性算法相关问题解析

---

## 1. 对于有向图、有权图，公式哪些参数需要改变？

### 有向图
- **邻接矩阵不再对称**：\( $a_{ij}$ \) 表示从 \( j \) 到 \( i \) 的边（或相反，取决于约定）。通常采用**行随机**的权重设计，即每个节点的权重只由其出度（或入度）决定。
- **权重定义修改**：
  \[
  $w_{ij} = \frac{a_{ij}}{\sum_{k=1}^N a_{ik}} \quad \text{（仍按出度归一化，但此时矩阵可能不对称）}$
  \]
  为保证收敛，通常要求图**强连通**且**非周期**，权重矩阵为行随机（每行和为1）且不可约。

### 有权图
- **边权已知**：若每条边 \( (i,j) \) 有正权重 \( $e_{ij} \$)，则权重可直接取为
  $\[
  $w_{ij} = \frac{e_{ij}}{\sum_{k=1}^N e_{ik}} \quad \text{（行归一化）}$
  ]
  或采用其他满足行和为1的设计（如 Metropolis 权重等）。此时无需再依赖邻接矩阵的 0/1 指示。

---

## 2. 推导公式的矩阵形式

将所有智能体的状态组成列向量：
\[
$\mathbf{x}^{(t)} = \begin{pmatrix} x_1^{(t)} \\ x_2^{(t)} \\ \vdots \\ x_N^{(t)} \end{pmatrix}$
\]
定义权重矩阵 \( $W \in \mathbb{R}^{N \times N}$ \)，其第 \( i \) 行第 \( j \) 列元素为 \( $w_{ij}$ \)。则迭代公式可写为：
\[
$\mathbf{x}^{(t+1)} = W \mathbf{x}^{(t)}, \quad t = 0, 1, 2, \dots$
\]
由此递推得：
\[
$\mathbf{x}^{(t)} = W^t \mathbf{x}^{(0)}$
\]
其中 \( $\mathbf{x}^{(0)}$ \) 为初始状态向量。

---

## 3. 所有智能体状态最终趋于一致，一致值为多少？在各种因素影响下，智能体状态还会趋于一致吗？

### 一致值
- **无向连通图 + 等权重（上述定义）**：权重矩阵 \( W \) 是**双随机**（每行和每列和均为1），且不可约非周期。此时所有智能体状态收敛到初始状态的平均值：
  \[
  $x_i(\infty) = \frac{1}{N} \sum_{j=1}^N x_j^{(0)} \quad \forall i$
  \]
- **有向强连通非周期图**：若 \( W \) 是行随机且不可约，则收敛到初始状态的加权平均，权重由 \( W \) 的左特征向量（稳态分布）\( $\pi$ \) 决定：
  \[
  $x_i(\infty) = \pi^{\mathsf{T}} \mathbf{x}^{(0)} = \sum_{j=1}^N \pi_j x_j^{(0)}$
  \]
  其中 \( $\pi$ \) 满足 \( $\pi^{\mathsf{T}} W = \pi^{\mathsf{T}}$ \)，且 \( $\sum \pi_j = 1$ \)。

### 影响因素
- **通信因素**：延迟、丢包、量化误差、异步更新等可能破坏一致性或减慢收敛速度。若图连通性得以保持且算法适当修改（如引入补偿机制），仍可能收敛，但一致值可能不再为均值。
- **拓扑变化**：动态图（边随时间变化）若满足联合连通性条件，仍可达成一致。
- **恶意攻击**：若存在拜占庭节点，传统一致性算法可能无法收敛。

在理想条件下（固定连通图、无干扰、权重设计合理），状态会趋于一致；一旦引入干扰，需要针对性设计鲁棒算法才能维持收敛。

---

## 4. 离散时间和连续时间下算法的异同

| 方面          | 离散时间                                                     | 连续时间                                                     |
| ------------- | ------------------------------------------------------------ | ------------------------------------------------------------ |
| **模型**      | $( x_i^{(t+1)} = \sum_j w_{ij} x_j^{(t)} )$                  | \( $\dot{x}_i(t) = \sum_j a_{ij} (x_j(t) - x_i(t))$ \)       |
| **矩阵形式**  | \( $\mathbf{x}(t+1) = W \mathbf{x}(t)$ \)                    | \( $\dot{\mathbf{x}}(t) = -L \mathbf{x}(t)$ \)，\( L \) 为图拉普拉斯 |
| **解的形式**  | \( $\mathbf{x}(t) = W^t \mathbf{x}(0)$ \)                    | \( $\mathbf{x}(t) = e^{-L t} \mathbf{x}(0)$ \)               |
| **收敛条件**  | 图连通，\( W \) 为行随机且不可约非周期                       | 图连通（拉普拉斯矩阵半正定，零特征值重数为1）                |
| **收敛值**    | 离散时间的平均一致性需 \( W \) 双随机 → 算术平均；有向图 → 加权平均 | 连通无向图 → 算术平均；有向图 → 加权平均（若拉普拉斯满足平衡条件） |
| **步长/时间** | 固定时间步，收敛速度取决于 \( $\rho(W - \frac{1}{N}\mathbf{1}\mathbf{1}^{\mathsf{T}})$ \) | 连续演化，收敛速度取决于拉普拉斯的第二大特征值               |
| **联系**      | 连续时间算法可通过欧拉离散化得到离散形式，但需注意步长选择以保证稳定性和一致性 |                                                              |

**异同总结**：
- **相同点**：两者均依赖图连通性，最终都使状态趋于一致（均值或加权均值）；均可用于分布式平均共识。
- **不同点**：离散时间适用于周期性更新，连续时间适合模拟物理过程或设计微分控制器；离散时间权重矩阵必须满足行和为1，连续时间则通过拉普拉斯矩阵实现“向邻居平均”的演化；收敛速度和稳定性分析工具不同（谱半径 vs. 矩阵指数）。

